# Project Rules

以后每次任务默认遵守：

1. 不要长篇解释。
2. 不要重复项目背景。
3. 不要输出完整文件，除非必须。
4. 只改和任务直接相关的文件。
5. 修改后运行必要检查。
6. 修改后运行 `npm run build`。
7. 如果任务完成，请提交到 GitHub `main`。
8. 回复只包含：
   - 修改文件
   - 检查结果
   - commit 信息
   - 是否等待 Netlify 自动部署

禁止事项：

- 不要改 Netlify 配置，除非任务明确要求。
- 不要新增后端，除非任务明确要求。
- 不要编造口语题、单词或听写材料。
- 不要写假的 AI 评分。
